extern crate iso_3166;
use iso_3166::country::*;
use std::convert::From;
// use strum::IntoEnumIterator;

fn main() {
    // let country = Info::from(Alpha2::SI);
    // println!("{:#?}", country);

    // for country in Info::all() {
    //     println!("{} - {:?}", country.name, country.language_codes);
    // }

    // for alpha2 in Alpha2::iter() {
    //     println!("{:?}", alpha2);
    // }
}
